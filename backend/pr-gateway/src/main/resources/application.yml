server:
  port: 8080

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka

spring:
  cloud:
    gateway:
      routes:
        - id: pr-esp
          uri: lb://pr-esp/esp
          predicates:
            - Path=/esp/**
  application:
    name: gateway-service
